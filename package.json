{
  "name": "earth6k",
  "version": "1.0.2",
  "private": true,
  "scripts": {
    "start": "node ./server.js",
    "dev": "nodemon ./server.js",
    "build": "mkdir -p public/library && cp -r node_modules/@fortawesome/fontawesome-free public/library/fontawesome",
    "test": "echo \"No tests specified.\" && exit 0"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.15.4",
    "compression": "^1.8.0",
    "express": "^4.21.2",
    "hbs": "^4.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  },
  "engines": {
    "node": ">=22"
  }
}
